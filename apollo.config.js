module.exports = {
  service: {
    endpoint: {
      url: 'http://localhost:3030/graphql', // defaults to http://localhost:4000
      skipSSLValidation: true, // optional, disables SSL validation check
    },
  },
};
